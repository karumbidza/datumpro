/** Tailwind CSS v4 — configured via the PostCSS plugin; design tokens live in
 *  app/globals.css (CSS-first config, no tailwind.config needed). */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
